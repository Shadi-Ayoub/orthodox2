"""
Keep the schema type definition only in this file
"""
schema {
	query: Query
	mutation: Mutation
}

type Setting {
	id: String!
	settings: String!
}

type SettingType {
	id: String
	settings: String
}